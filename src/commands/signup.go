package commands

import (
	"github.com/bwmarrin/discordgo"
)

// Signup command is deprecated - handlers are now in handlers/reaction.go
func SignupCommand(s *discordgo.Session, m *discordgo.MessageCreate) {
	// This function is no longer used
}
